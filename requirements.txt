# V2 only — the V1 package cannot sign orders the post-2026-04-28 CLOB
# accepts, and importing py_clob_client_v2 against it fails at startup.
# Keep in sync with pyproject.toml: this file is what the deploy installs.
py-clob-client-v2>=1.1.0
python-dotenv>=1.0.0
pydantic>=2.0
httpx>=0.27
websockets>=12.0
numpy>=1.26
pandas>=2.2
structlog>=24.0
fastapi>=0.104
uvicorn[standard]>=0.24
pywebpush>=2.0
